@page "/test/wap.wml"
@model OperaLegacyLab.Web.Pages.Test.WapWmlModel
@* OnGet always returns Content(...) directly with real WML/XML bytes -
   see code-behind. This markup never actually renders. *@

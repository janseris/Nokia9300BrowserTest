@page "/test/encoding/latin1"
@model OperaLegacyLab.Web.Pages.Test.EncodingLatin1Model
@* OnGet always returns Content(...) directly with real ISO-8859-1 bytes -
   see code-behind. This markup never actually renders. *@
